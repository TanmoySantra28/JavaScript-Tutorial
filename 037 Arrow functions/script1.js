//we can send some arguments to an arrow function
const hello = (name) => console.log(`Hello ${name}`);
hello("MB");

//To write more than one statements or lines, use parenthesis
const hi = (name,age) => {
    console.log(`Hi ${name}`);
    console.log(`You are ${age}`);
};
hi("MB",20);
